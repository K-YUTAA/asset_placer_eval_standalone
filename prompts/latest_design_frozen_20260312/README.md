# Latest design frozen prompts (2026-03-12)

- This directory snapshots the prompt set used by the parser-consistent latest-design config.
- It is intended for `experiments/configs/pipeline/latest_design_v3_gpt_high_frozen_20260312.json`.
- `step1_openai_prompt.txt` is a frozen copy of the Step1 prompt used by the latest-design line.
- `gemini_furniture_prompt.txt` is the frozen furniture detection prompt used by the latest-design line.
- `gemini_room_inner_frame_prompt.txt` is a frozen copy of the current code default room inner frame prompt.
- `gemini_openings_prompt.txt` is a frozen copy of the current code default openings prompt.
